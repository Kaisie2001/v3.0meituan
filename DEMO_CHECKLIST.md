# Demo 人工验收清单

> 交付前按顺序走一遍。建议在 **390px 移动端视口**（或真机）下验收。  
> 自动化覆盖见 `npm run test:e2e`（3 条黄金路径）。

## 环境准备

- [ ] Node.js 18+ 已安装
- [ ] `npm install` 已完成
- [ ] `npm run dev` 启动成功，浏览器打开 `http://localhost:3000`
- [ ] 若 E2E：`npm run test:e2e`（webServer 使用 `127.0.0.1:3100`）

---

## 1. 首页 · 输入目标

- [ ] 首页展示「成行地图」与目标输入框（`goal-input`）
- [ ] 可手动输入自然语言，例如：「晚上和朋友吃饭，吃完想找地方聊天，人均150以内」
- [ ] 点击「快捷入口」中任一场景（如「朋友聚会」「顺路办事」）会自动填入对应 goal
- [ ] 「开始规划」按钮可点击（`run-agent-button`）

## 2. 修改出行设置

- [ ] 点击「出行偏好」行（`travel-settings-button`）打开底部 Sheet
- [ ] 可修改：日期 / 出发时间 / 时长 / 出行方式 / 路线优先级
- [ ] 「人数与预算」可展开修改
- [ ] 点击「保存」后 Sheet 关闭，首页摘要更新（如「今天 18:30 · 3小时 · 地铁公交 · 少排队」）

## 3. 生成主方案

- [ ] 点击「开始规划」后出现规划 Loading（约 2s）
- [ ] 进入结果页（`result-screen`）：地图在上、方案 Sheet 在下
- [ ] 主方案 Tab（`main-plan-tab`）可见
- [ ] 路线三步（`route-step-list`）至少 1 步，显示 POI 名称
- [ ] 决策卡展示方案名、成行相关指标、出行偏好摘要
- [ ] 「确认并执行」「查看备选方案」按钮可见

## 4. 切换备选方案

- [ ] 点击「备选方案」Tab 或「查看备选方案」
- [ ] 至少 1 张备选卡（`fallback-card`）可见（朋友晚间场景应稳定出现）
- [ ] 备选卡展示：替换节点 / 解决什么问题 / 代价
- [ ] 点击「选择此方案」（`select-fallback-button`）后回到主 Tab
- [ ] 主方案区显示已切换为备选（或切换反馈条）
- [ ] 「恢复主方案」可用

## 5. 点击推荐点

- [ ] 点击「推荐点」Tab（`poi-tab`）
- [ ] 至少 1 个推荐点（`poi-card` / `selected-poi-detail`）可见
- [ ] 展示 POI 名称、成行分、推荐理由或风险提示
- [ ] 点击地图 Marker（若可见）可切换当前 POI 详情

## 6. 地图随方案变化

- [ ] 地图容器（`map-container`）可见，底图或占位正常
- [ ] 路线折线与推荐点 Marker 可见
- [ ] 切换主方案 ↔ 备选后，地图高亮/路线有变化（或 hint 文案变化）
- [ ] 若 OSM 瓦片加载失败，显示「底图加载失败」说明（非空白崩溃）

## 7. 确认执行

- [ ] 主方案 Tab 点击「确认并执行」（`confirm-execute-button`）
- [ ] 进入执行页（`execution-screen`）
- [ ] 展示当前将执行的计划摘要
- [ ] 点击「确认执行」（`execution-start-button`）开始模拟

## 8. 执行完成页

- [ ] 执行进度完成后出现「安排已完成」（`execution-done`）
- [ ] 展示已完成动作摘要列表
- [ ] 预订凭证卡（`booking-voucher`）可见（含 mock 二维码/预约信息）
- [ ] 「发给同行人」文案区可展开，复制按钮可用
- [ ] 「技术细节」默认折叠，展开后可看 mock tool 调用记录

## 9. 返回首页 / 重置演示

- [ ] 执行页「← 返回方案」回到结果页
- [ ] 结果页「← 返回」回到首页
- [ ] 首页「清空」重置 goal / 出行设置 / 场景选中状态
- [ ] 重置后可重新规划，流程不报错

---

## 10. 异常与边界状态

| 场景 | 预期 |
|------|------|
| 关键字段缺失（极少触发） | Clarify 弹窗「再确认几个细节」，可补全后继续 |
| 无备选方案 | 备选 Tab 提示「主方案已经比较稳妥」，不空白 |
| 无推荐点 | 推荐 Tab 提示「暂无推荐点详情」，不 crash |
| 底图网络失败 | 地图区说明性错误文案，页面其余功能可用 |
| 空 goal 规划 | 使用默认 goal 或启发式兜底，不出现 undefined/NaN |
| 修改出行设置（结果页） | 方案随 `applyTravelSettingsToPlan` 后处理更新，不重新跑完整 Agent |

---

## 自动化回归（交付前必跑）

```bash
npm run test        # 75 项单元/集成测试
npm run test:e2e    # 4 项 Playwright 黄金路径
npm run build       # 生产构建
```

全部通过后再对外 Demo。

---

## 推荐演示场景（与 E2E 对齐）

| 场景 | 快捷入口 | 演示要点 |
|------|---------|---------|
| 朋友聚会 | 朋友聚会 | 晚高峰、备选方案、少排队 |
| 顺路办事 | 顺路办事 | 顺路节点、主方案 → 执行闭环 |
| 工作学习 | 工作学习 | 推荐点 Tab、安静场景 |
